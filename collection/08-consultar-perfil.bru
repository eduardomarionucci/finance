meta {
  name: Consultar perfil
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/perfil/{{perfilId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
}
