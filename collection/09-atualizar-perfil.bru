meta {
  name: Atualizar perfil
  type: http
  seq: 9
}

put {
  url: {{baseUrl}}/perfil
  body: form-urlencoded
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:form-urlencoded {
  id: {{perfilId}}
  description: Perfil atualizado
}

assert {
  res.status: eq 200
}
